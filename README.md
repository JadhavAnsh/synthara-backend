# Backend

This folder contains the `uv`-initialized Python backend scaffold for Synthara Research.

Recommended next steps:

- run `uv sync`
- add Appwrite configuration and repositories
- build the FastAPI request, job, and result endpoints
- implement orchestration, agents, and formatters under `app/`
